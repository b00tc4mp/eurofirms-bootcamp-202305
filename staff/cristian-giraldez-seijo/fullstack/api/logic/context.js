/* This code is creating a JavaScript object called `context` with two properties: `users` and `posts`.
Both properties are initially set to `null`. */
const context = {
    users: null,
    posts: null
}

module.exports = context 